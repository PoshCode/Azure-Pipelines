parameters:
  Path: .\RequiredModules.psd1
  Destination: .\RequiredModules

steps:
- powershell: |
    Save-Script Install-RequiredModule -Path .\Install-RequiredModule.ps1
    .\Install-RequiredModule.ps1 -Path '$(Path)' -CleanDestination '$(Destination)'
  displayName: 'Restore pre-requisites'
