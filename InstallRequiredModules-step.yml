parameters:
  Path: ./RequiredModules.psd1
  Destination: ./RequiredModules

steps:
- powershell: |
    Save-Script Install-RequiredModule -Path .
    ./Install-RequiredModule.ps1 -Path '${{ parameters.Path }}' -Destination '${{ parameters.Destination }}' -Verbose
  displayName: 'Restore pre-requisites'
